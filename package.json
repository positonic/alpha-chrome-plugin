{
  "name": "alpha-chrome-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Alpha Trader Chrome extension with local Whisper speech recognition",
  "scripts": {
    "build": "bash build.sh"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.4.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
